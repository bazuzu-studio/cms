# [Dokploy] Зависимости и артефакты сборки — ставятся/собираются внутри образа
node_modules
.next
out
build
coverage

# VCS / IDE
.git
.gitignore
.vscode
.idea

# Секреты и локальные окружения — никогда не попадают в образ
.env
.env.*
!.env.example
dokploy.env

# Тесты и служебное
tests
test-results
playwright-report
playwright
*.md
Dockerfile
.dockerignore
docker-compose*.yml
npm-debug.log*
.DS_Store
